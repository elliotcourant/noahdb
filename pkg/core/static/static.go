package static

import (
	"time"
)

func init() {
	time.Sleep(100 * time.Millisecond)
	// statikFS, err := fs.New()
	// if err != nil {
	// 	panic(err)
	// }
	// file, err := statikFS.Open("internal_sql.sql")
	// if err != nil {
	// 	panic(err)
	// }
	// data := make([]byte, 0)
	// _, err = file.Read(data)
	// if err != nil {
	// 	panic(err)
	// }
}
